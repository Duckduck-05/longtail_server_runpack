# Runtime dependencies for the vendored sources on a modern CUDA/PyTorch host.
# Do not install the obsolete third_party requirements verbatim: CBDM's 2021
# file pins TensorFlow 2.4 and PyTorch 1.7, which would replace a server's
# working CUDA build despite neither being imported by the training path.
absl-py==2.1.0
scipy==1.13.1
scikit-learn==1.5.2
tensorboardX==2.6.2.2
tqdm==4.67.1
Pillow==10.4.0
PyYAML==6.0.2
psutil==6.1.1
jsonschema==4.23.0
wandb==0.19.6
wandb-workspaces==0.1.9
pytest==8.3.4
# None of the vendored method repos ship their own requirements file, so
# their hard, unconditionally-imported deps are pinned here instead.
pytorch-metric-learning==2.7.0   # coral-lt-diffusion/diffusion.py
scikit-image==0.24.0             # CBDM-pytorch/utils/augmentation.py, also
                                  # reached via symlink by coral/OC_LT/ImbDiff-CM
matplotlib==3.9.2                # CBDM-pytorch/score/prd_score.py, same symlink fan-out
ipdb==0.13.13                    # OC_LT/diffusion.py and friends
